# Note: The agent profile configuration is now defined as a Pydantic model
# named 'SerializableAgentProfile' within 'ciris_engine/core/config_schemas.py'.
# This allows agent profiles to be loaded and saved as part of the main application configuration.

# The previous dataclass definition has been removed from this file.
import logging

logger = logging.getLogger(__name__)

pass
