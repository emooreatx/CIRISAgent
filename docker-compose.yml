version: '3.8'
services:
  cirisagent:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: python main.py --modes discord --profile teacher