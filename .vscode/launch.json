{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Debug Pytest",
      "type": "debugpy",  // Updated to use the new debugger
      "request": "launch",
    //   "program": "${workspaceFolder}/final_test_reddit.py",
      "module": "pytest",
      "args": [
        "${workspaceFolder}/final_test_reddit.py",  // Path to your test file
        "--maxfail=1",  // Stop after the first failure
        "--disable-warnings"  // Disable warnings for cleaner output
      ],
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
